PERFORMANCE IS KEY.
Always clarify implementation strategy; do not guess.
Prefer TypeScript/Bun for repo-local skill scripts; use Python only when TypeScript is a poor fit.
Prefer `rg --files` for file discovery and `rg` for exact or broad content lookup.
Use direct file reads once the target file is known.
Do not add MCP search indirection when a local search command will answer faster.
Document intent, boundaries, and aesthetic direction, not code-searchable facts.
Update the nearest README whenever behavior, workflow, or visual direction changes meaningfully.
Treat source images as structural input, not literal final output.
Preserve hero-first composition and audio-reactive continuity.
Prefer legibility and continuity over piling on more effects.
