{
  "name": "ai-video",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/cli.ts",
    "fiesta": "bun run scripts/fiesta.ts",
    "check": "tsc --noEmit",
    "test": "bun test",
    "test:coverage": "bun test --coverage --coverage-reporter=text --coverage-reporter=lcov",
    "test:coverage:check": "node scripts/check-coverage.mjs"
  },
  "dependencies": {
    "@napi-rs/canvas": "^0.1.77",
    "commander": "^14.0.1",
    "ffmpeg-static": "^5.2.0",
    "ffprobe-static": "^3.1.0",
    "sharp": "^0.34.2",
    "wabt": "^1.0.39",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "bun-types": "^1.3.2",
    "typescript": "^5.9.2"
  }
}
